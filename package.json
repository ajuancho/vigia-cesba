{
  "name": "vigia",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma de inteligencia legislativa y regulatoria argentina",
  "scripts": {
    "dev": "pnpm --filter ./apps/web dev",
    "build": "pnpm --filter ./apps/web build",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
